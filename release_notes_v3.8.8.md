# TTS Studio v3.8.8

- **Renamed to TTS Studio.** The app is no longer local-only — it can offload
  synthesis to a shared remote server — so "Local" has been dropped from the
  name throughout the UI, window title, and app bundle. No functional change;
  your projects, profiles, and settings are untouched.

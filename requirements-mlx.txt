# The MLX generation worker's own environment (mlx_worker.py) — Apple Silicon
# only. Separate from requirements.txt because mlx-audio needs
# transformers>=5.14 while qwen-tts pins transformers==4.57.3.
#   python3 -m venv venv-mlx && ./venv-mlx/bin/pip install -r requirements-mlx.txt
# then run the app with QWEN_TTS_MLX_PYTHON=$PWD/venv-mlx/bin/python.
# Versions are the ones the A/B test ran on (2026-09-23).
mlx==0.32.2
mlx-audio==0.5.5
soundfile
numpy

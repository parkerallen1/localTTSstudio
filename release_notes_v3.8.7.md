# Local TTS Studio v3.8.7

- **Downloads no longer upload anything on saved projects.** The Download
  button now tells the server which stored segments to merge instead of
  uploading them, so slow wifi can't stall the export. The browser only
  downloads the finished file. (Unsaved projects still upload from memory.)
